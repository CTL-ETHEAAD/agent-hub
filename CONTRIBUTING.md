# Contributing

Use Node.js 20 or newer. Keep changes scoped, add focused tests, and run `npm test` before submitting a change.

Never commit runtime state, credentials, private source material, company-specific repository profiles, or absolute workstation paths.
